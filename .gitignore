node_modules/
dist/
.vercel/
.env
.env.*
!.env.example

# Impeccable : outil de design, reinstallable via `npx impeccable install`
# (on ne versionne pas 148 fichiers tiers dans les diffs du projet)
.claude/skills/
.claude/settings.local.json
